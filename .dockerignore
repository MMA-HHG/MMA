# Keep the Docker build context minimal.
# The MMA source tree is mounted later at runtime with -v .:/MMA.

**

!Dockerfile
!.dockerignore
!requirements.txt
!docker/
!docker/entrypoint.sh
!docker/mma-build
!docker/mma-jupyter
